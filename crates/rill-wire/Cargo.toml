[package]
name = "rill-wire"
description = "Framed I/O for the Rill protocol: async read/write of frames plus direction checks and the debug frame dump."
edition.workspace = true
version.workspace = true
license.workspace = true

[dependencies]
rill-protocol = { path = "../rill-protocol" }
tokio = { workspace = true, features = ["io-util"] }

[lints]
workspace = true
